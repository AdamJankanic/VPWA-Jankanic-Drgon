import type { MatchedNamespace, NamespaceJSON, NamespacesTree } from '@ioc:Ruby184/Socket.IO/Ws';
export declare class Store {
    tree: NamespacesTree;
    add(nsp: NamespaceJSON): this;
    statics(): string[];
    isDynamic(name: string): boolean;
    match(name: string): null | MatchedNamespace;
}
